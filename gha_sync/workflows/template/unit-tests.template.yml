---
name: Unit Tests

on:
  workflow_dispatch:
  merge_group:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

concurrency:
  group: '{{ concurrency_group }}'
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  unit-tests:
    name: Unit Tests
    uses: worganisation/github-config-files/.github/workflows/__unit-tests.yml@0.8.7
    with:
      python-version: '{{ python_version }}'
      source-dir: '{{ source_dir }}'
    secrets: inherit
